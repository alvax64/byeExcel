# Copy this file to byeexcel.env in the deployment secrets directory.
# Keep real values out of Git. `export` makes each value available to Jac
# when this file is sourced by the deployment launcher.

export JWT_SECRET=
export ANTHROPIC_API_KEY=
export BYLLM_DEFAULT_MODEL=claude-opus-5
